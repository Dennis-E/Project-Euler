for a in range(999):
	for b in range(a):
		c=1000-a-b
		if a**2+b**2==c**2: print(str(a)+" "+str(b)+" "+str(c))
		
#[(x,y,1000-x-y) for x in range(1,1000) for y in range(1,x) if x**2+y**2==(1000-x-y)**2]